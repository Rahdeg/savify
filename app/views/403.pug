extends layout

block content
  section(class='space-y-4 text-center py-16')
    h1(class='text-2xl font-semibold text-red-600') 403 — Access Denied
    p(class='text-slate-600') You do not have permission to view this page.
    a.link-text(href='/dashboard') Return to Dashboard
